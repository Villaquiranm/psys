#include "sysapi.h"

int main(void *arg)
{
        (void)arg;

//        pcreate(10);

        /*char str[10];
        //"Hello\0";
        strncpy(str,"Hello\0",5);
        cons_write(str,strlen(str));

        cons_write("Salut\0",6);

        //printf("S: %s\n", str);
        //printf(str);

        if(str==NULL)
          return 32832;
        else*/
          return 32832;
}
